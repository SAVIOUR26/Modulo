import 'package:flutter/material.dart';

/// Modulo's palette — a dark stage-gear look (near-black walnut casing,
/// brass control knobs, ivory key text) rather than a generic app theme.
/// Chosen to feel like a physical instrument accessory, since that's how
/// it's actually used: glanced at mid-song, not browsed like a typical app.
class AppColors {
  static const Color background = Color(0xFF15120D);
  static const Color panel = Color(0xFF1D1811);
  static const Color panelEdge = Color(0xFF2B2418);
  static const Color ink = Color(0xFFF0E6D2);
  static const Color inkDim = Color(0xFFA6987C);
  static const Color brass = Color(0xFFD9A53A);
  static const Color brassDim = Color(0xFF8A6A26);
  static const Color teal = Color(0xFF5B968E);
  static const Color minor = Color(0xFF8F8367);
  static const Color danger = Color(0xFFB45B4C);
  static const Color chordCell = Color(0xFF241D14);
}
